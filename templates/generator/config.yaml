# Template Generation Configuration
# Defines tool-specific variables for template generation

tools:
  claude:
    name: "claude"
    display_name: "Claude Code"
    yaml_frontmatter: true
    permissions_enabled: true
    hooks_enabled: true
    config_file: "claude-code-settings.json"
    mcp_config: ".mcp.json"
    commands_dir: "commands"
    agents_dir: "agents"
    settings_dir: "$HOME/.claude"

  codex:
    name: "codex"
    display_name: "Codex"
    yaml_frontmatter: false
    permissions_enabled: false
    hooks_enabled: true
    config_file: "config.toml"
    mcp_config: null
    commands_dir: null
    agents_dir: "agents"
    settings_dir: "$HOME/.codex"

  cursor:
    name: "cursor"
    display_name: "Cursor CLI"
    yaml_frontmatter: false
    permissions_enabled: false
    hooks_enabled: false
    config_file: null
    mcp_config: "mcp.json"
    commands_dir: "commands"
    agents_dir: "agents"
    settings_dir: "$HOME/.cursor"

# Common variables available to all templates
common:
  project_context_file: "PROJECT_CONTEXT.md"
  agents_file: "AGENTS.md"
  context_file: "CONTEXT.md"
  tidewave_port_template: "{{PORT}}"

harness:
  planner-phoenix:
    claude: { model: opus, effort: high }
    codex: { model: gpt-5.5, effort: high }
    cursor: { model: opus-thinking-high, effort: high }
  planner-html:
    claude: { model: opus, effort: high }
    codex: { model: gpt-5.5, effort: high }
    cursor: { model: opus-thinking-high, effort: high }
  planner-hugo:
    claude: { model: opus, effort: high }
    codex: { model: gpt-5.5, effort: high }
    cursor: { model: opus-thinking-high, effort: high }
  planner-vite:
    claude: { model: opus, effort: high }
    codex: { model: gpt-5.5, effort: high }
    cursor: { model: opus-thinking-high, effort: high }
  developer-phoenix-backend:
    claude: { model: sonnet, effort: medium }
    codex: { model: gpt-5.5, effort: medium }
    cursor: { model: sonnet, effort: medium }
  developer-phoenix-frontend:
    claude: { model: sonnet, effort: medium }
    codex: { model: gpt-5.5, effort: medium }
    cursor: { model: sonnet, effort: medium }
  developer-html:
    claude: { model: sonnet, effort: high }
    codex: { model: gpt-5.5, effort: medium }
    cursor: { model: sonnet, effort: medium }
  developer-hugo:
    claude: { model: sonnet, effort: high }
    codex: { model: gpt-5.5, effort: medium }
    cursor: { model: sonnet, effort: medium }
  developer-vite:
    claude: { model: sonnet, effort: high }
    codex: { model: gpt-5.5, effort: medium }
    cursor: { model: sonnet, effort: medium }
  reviewer-phoenix:
    claude: { model: sonnet, effort: medium }
    codex: { model: gpt-5.5, effort: medium }
    cursor: { model: sonnet, effort: medium }
  reviewer-static:
    claude: { model: sonnet, effort: medium }
    codex: { model: gpt-5.5, effort: medium }
    cursor: { model: sonnet, effort: medium }
  committer:
    claude: { model: haiku, effort: medium }
    codex: { model: gpt-5.5-mini, effort: medium }
    cursor: { model: haiku, effort: medium }
  build:
    claude: { model: haiku, effort: medium }
    codex: { model: gpt-4.1-mini, effort: medium }
    cursor: { model: haiku, effort: medium }
  inspector:
    claude: { model: sonnet, effort: medium }
    codex: { model: gpt-5.5, effort: medium }
    cursor: { model: sonnet, effort: medium }
