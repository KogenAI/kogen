The original v1 adapter accepted string-valued scopes; the source correction requires an object with a list of strings. Original observed failure remains in runtime compact-malformed-scope/original.json. Positive scoped seed and malformed-string negatives passed. Unchanged reader/other v1 controls remain source-bound evidence; prerequisite-results.json is that historical v1 receipt, not a claim every route reran under v2. These are fixture controls, not model quality.
