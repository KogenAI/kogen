# Manifest Tracks @external_resource

If Elixir @external_resource a file, add path to manifest hash. Otherwise file edits don't invalidate stamps → stale compilation.

Grep @external_resource → cross-check manifest.
