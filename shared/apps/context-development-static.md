# Development — Stack, Build, Conventions

Stub seeded by `codegen-scaffold` on first provision. The app's own agents own this file from the
first build onward — replace this content as the app's real conventions take shape.

## Stack

- **Stack**: Vite (static site) + Tailwind v4
- **Build**: `npm run build` → `public/`
- **Dev server**: `npm run serve`

## Conventions

(none yet — first build populates this)

## Testing

(none yet — first build populates this)

## Env Vars

(none yet — first build populates this)

## CI

`make ci` — the app's own gate. See `Makefile` for the target definition.
