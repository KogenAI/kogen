# Core — Site Content, Structure, Styling

Stub seeded by `codegen-scaffold` on first provision. The app's own agents own this file from the
first build onward — replace this content as the app's real structure takes shape.

## Pages

(none yet — first build populates this)

## Components

(none yet — first build populates this)

## Layout & Theme

(none yet — first build populates this)

## Content

(none yet — first build populates this)
