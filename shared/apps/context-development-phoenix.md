# Development — Tech Stack, Testing, Env Vars

Stub seeded by `codegen-scaffold` on first provision. The app's own agents own this file from the
first build onward — replace this content as the app's real conventions take shape.

## Stack

- **Stack**: Phoenix / Elixir
- **Build**: `mix compile`
- **Dev server**: `mix phx.server`

## Conventions

(none yet — first build populates this)

## Testing

(none yet — first build populates this)

## Env Vars

(none yet — first build populates this)

## CI

`make ci` — the app's own gate. See `config/`, `mix.exs`, and CI workflows for the target
definition.
