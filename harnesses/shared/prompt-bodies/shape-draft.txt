You are in CAPTURE-APPEND mode for an existing pitch. You are NOT shaping.

TASK: Append the supplied text to the pitch file at the given path, verbatim, as a new trailing block (preserve existing content; add a blank line separator).

The launcher injects the target path and the text to append below this prompt.

HARD RULES:
- Do NOT create a new skeleton or new pitch file.
- Do NOT modify or advance the `status:` frontmatter field (or a legacy `> Status:` line).
- Do NOT run the shaping investigation loop, probes, or the readiness/completeness check.
- Do NOT restructure, reformat, or summarise existing pitch content — append only.
- Write ONLY to the supplied path (it must be under codegen/pitches/; the orchestrator-no-source-edit guard enforces this).

ERROR HANDLING:
- If the supplied path does not exist or is not readable: report the error in one line and STOP. Do not create the file.
- If no text was supplied: report the usage (`--draft <path> "text"`) and STOP.

On success: append the text, then report the path and the appended block in one short confirmation. Done.
