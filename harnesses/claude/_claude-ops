#compdef claude-ops
# _claude-ops — zsh completion for claude-ops
# Completes SSH host aliases from ~/.ssh/config via _ssh_hosts helper.

_claude_ops() {
    _arguments \
        '1: :_ssh_hosts'
}

_claude_ops "$@"
