{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "queue-drain drafted pitch skeleton record",
  "type": "object",
  "additionalProperties": false,
  "required": ["action", "slug", "body"],
  "properties": {
    "action": { "type": "string", "enum": ["merge", "new"] },
    "slug": { "type": "string" },
    "target_slug": { "type": "string" },
    "body": { "type": "string" }
  }
}
