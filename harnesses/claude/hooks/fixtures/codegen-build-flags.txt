# Public flag surface of codegen-build. A diff here IS the review signal —
# update deliberately in the same commit; never delete the assertion.
--cwd
--effort
--fallback-model
--harness
--help
--max-budget-usd
--model
--print-argv
--print-dir
--stack
