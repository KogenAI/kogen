READ-ONLY AUDIT. Do not modify, create or delete any file. Do not run mix compile, mix test, make, or any git command that writes. Only read files and run read-only commands (cat, sed, grep, rg, ls, git show, git log, git diff).

Audit the Kogen Intent package .kogen/intents/drafts/fortify-paid-verification/ (intent.yaml, INTENT.md, scenarios.yaml, risks.yaml, questions.md, approval.md, references.yaml) in this checkout, which is main at 363c20af. The quality bar and format are priv/kogen/prompts/shaping.md. Nothing else lands before this Build (shaping-preflight-audit is parked and builds later; this package must not depend on its code). The Build runs under main's controller at 363c20af with `mix kogen.build fortify-paid-verification --route claude-dominant-adversarial-codex` (Claude Code Developer, Codex GPT-6 Sol Reviewer and Expert); .kogen/config.yaml keeps default_route: claude, so nested live fixtures run on claude unless they pass --route.

Find, with file:line evidence at HEAD:
1. Infeasible or self-contradicting scenarios, including contradictions with the code at HEAD (anchors, function names, arities, layouts cited in the package).
2. Per scenario, a plausible wrong implementation that passes its named proofs (proof.offline selectors plus the paid target).
3. Missing guarded paths, wrong or nonexistent selectors.
4. Conflicts with main's controller running this Build: catalog byte-freeze (priv/kogen/verification_targets.yaml, Makefile), Stop and PreToolUse scripts and registrations, VerificationPolicy.preflight, Candidate role prompts rendered at each launch, Contract.verdict exact keys, Kogen.Harness.Verdict.schema passed to the Codex Reviewer via --output-schema, rehearsal trace identities (scripts/check/rehearsals.exs), priv/kogen/test-reliability.yaml source hashes. Anything that assumes the Reviewer of this Build is Claude.
5. Unnecessary paid targets, or an edited catalog live-test owner whose target is not selected.
6. Scope creep beyond ROADMAP row 4: controller-owned verification, Candidate-bound receipts, failed-only paid reuse, no hardcoded check, in-Intent target additions, controller-run red-on-base proof tests, the Review ledger; live checks live-shape-to-build and live-native.
7. Build-failure lessons: (a) every new or changed persisted artifact traced through all consumers, including live-fixture retention and resolution after cleanup (Kogen.Build.Evidence.resolve/2, test/support/review_packet_audit.ex, test/support/live_rework_audit.ex, test/kogen/live_shape_to_build_test.exs, test/support/live_reviewer_rework_fixture.ex); (b) new code reading priv/ at runtime inside tests run from an isolated code copy; (c) live fixture wiring (route, profiles, logins, preflights) of each selected live target; (d) fixture or workspace copies inheriting .kogen/build.lock or runtime; (e) no plumbing without a caller: list everything the Intent adds and name its caller and proof in this Intent; (f) no unrequested splits: requested scope deferred without a recorded Shaper decision; (g) does every existing live case still reach its expected end state under the new behaviour?

Output: numbered findings, each with severity blocking or advisory, scenario id, file:line evidence and a concrete fix. Then a one-paragraph verdict. Be concise.
