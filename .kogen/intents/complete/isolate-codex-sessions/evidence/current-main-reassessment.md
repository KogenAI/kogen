# Current-main reassessment

Inspected main/2890d86f16c8819cfad12cbd98dbbdc8ee068b1c in the existing
September 13 visit. Provenance: after the controller recommended current-main
reassessment, the Shaper directed it to continue and objected to asking for routine
engineering direction. This authorizes the investigation and Draft integration,
not approval or implementation. Original shaped_against and historical
reshaped_against remain unchanged. The later accepted release-pinning revision is integrated below. This note owns the assessment of the newer
checkout; it does not retroactively attribute that assessment to an earlier visit.

## Conclusion and scope

The accepted public behavior still fits the same managed-runtime integration
contract. No new product choice was found. New consumers need explicit integration
and preservation coverage. Existing guarded paths cover that work. Do not restore
a failed implementation wholesale or replace newer maintained fixtures with older
prototype copies. Preserve one Build, two configured outer resumptions, current
profiles, public command syntax, scoped native login, named stdio execution and all
original isolation/preservation outcomes. Implementation readiness requires the
future Build's gates and Review; shaping evidence is not those results.

## Producer-to-consumer map

| Producer / starting state | Consumer and required integration | Existing evidence owner |
| --- | --- | --- |
| Explicit install and selected shared/project login | Fresh/continued Shape and Build preflight select the complete immutable runtime and effective scope before provider work | Public task tests and managed native acceptance; missing-state launch counters |
| Shape runtime/environment selection | Harness.exec_shaper and interactive child inherit managed discovery, configured profile and project context | Public Shape lifecycle and five-case evaluation, with real PTY capture |
| Build entry selection | Developer, exact resumes and each fresh Reviewer receive the original selected runtime/context despite pin/PATH changes | Build context, fake lifecycle and real rework owner |
| Native root execution | Configured helpers inherit managed discovery; stdio executor and tracked hooks restore caller tools without changing Codex storage | Compatibility root/helper/hook receipts and native helper/profile audit |
| Managed native session output | RootProfileAudit, NativeHelperFixture and five-case driver inspect the selected operation's actual retained session location | Outer live owners audit exact identities and retain concise evidence before cleanup |
| Five-case public Shape turn and scripted follow-up | Evaluation resume transport reuses that operation's absolute executable, account/session scope and managed launch context; never re-resolves PATH or personal sessions | Existing transport tests, driver rehearsal and five-case live owner |
| Required five-case artifacts | Existing single manifest passes isolated forwarding and Build TargetEvidence snapshot validation before Review | LiveShapingEvaluationTest, IsolatedTargetEvidence tests and Build target receipt |
| Candidate pin verification | One bounded compatibility runner receives candidate/runtime and selected authenticated scope explicitly | Build-owned native fixtures, offline state-transition controls and future live gate |

## Narrow engineering requirements

Bind selected runtime/context once at the operation boundary and carry it through
existing Harness routes. Keep KOGEN_HARNESS's explicit offline testing seam usable;
a fake executor proves orchestration only and is never managed native acceptance.
Do not interpret ordinary inherited PATH or provider/attachment overrides as an
alternative production runtime or account.

Expose operation-owned runtime/session locators to existing fixture owners through
private fixture plumbing, not a new public command or provider-session picker.
The five-case driver currently hardcodes Path.home()/.codex/sessions (driver.py:9)
and resume_transport.exp:16 invokes PATH codex. RootProfileAudit already accepts an
explicit sessions_root argument; pass the actual managed location. The native-helper live owner likewise derives
its session root from caller CODEX_HOME or personal HOME
(test/kogen/native_helper_live_test.exs:39-43); pass its actual selected managed
location into NativeHelperFixture.collect_receipt!/5. Preserve exact current native
root/helper profile evidence, not helper self-reported model names.

Fixture replies resume the already-launched fixture root. Public
mix kogen.shape <draft-slug> must still launch a fresh provider conversation and
preserve Draft identity/provenance. Do not conflate these two lifecycles. During a
fixture's scripted stop/resume, retain its immutable launch snapshot and selected
runtime until capture and final owned cleanup finish; stopping the first terminal
must not release the context needed by the next fixture turn. Production Shape
needs no new public recovery behavior.

Keep fixture account selection explicit before dispatch. Resolve the selected
already-authenticated Kogen scope from the caller checkout before creating private
fixtures, then pass it through the existing candidate/test boundary. No personal
credential import, fixture browser login or accidental project-to-shared fallback.
Synthetic offline scopes remain synthetic. Missing real setup is a prerequisite
failure with the existing explicit install/login guidance, not model readiness.
Do not parse or print token fields. The initial 0.154.0 candidate remains subject
to full Build compatibility; historical relocation and login observations are not
current setup checks.

Native session parsing belongs only to bounded test evidence consumers. Product
runtime compatibility and pin-change policy must not infer compatibility from private
rollout schema or version-string equality. Keep native originals private and
separate from concise required artifacts.

Preserve current concurrent isolated fixture execution and dependency-copy/process
ownership rules. Do not copy the prototype's async:false module into the suite,
reintroduce personal-session discovery to make capture pass, or serialize the
whole live suite to conceal cross-invocation state bugs. The prototype is advisory
source, not an implementation template for every current convention.

## Executed probe and retained limits

Ran `python3 -B .kogen/intents/drafts/isolate-codex-sessions/evidence/prototypes/current-consumers/probe.py`
from the repository root. The script imports the actual maintained driver and
creates only synthetic session files under its own temporary Intent-local directory.
With distinct synthetic HOME and CODEX_HOME, the default consumer misses the
managed session. A personal-location control is found. Injecting the managed
SESSION_ROOT makes the same production consumer find the managed session.
All three assertions passed; results.json binds the source SHA-256. Temporary
inputs were removed by the script. No native process, provider call, credentials,
production file write or gate was involved. This demonstrates a real consumer
mismatch and its existing injection seam, not complete managed transport.

The earlier denial prototype's 17 offline passes and 212.1-second native success
remain unchanged historical evidence. Its two required failed-Candidate source
files are absent on current main, so it cannot be rerun here as supplied. Do not
fabricate replacement dependencies, call that old result current acceptance, or
spend another provider run for an unchanged receipt. Its integration still requires
native stderr evidence bound to the invocation and session; model text, missing,
truncated, stale or mismatched receipts must fail.

## Acceptance sequence and challenge

Before provider execution, extend the existing offline driver rehearsal and
transport controls so only the native executor boundary is fake: exercise real
fixture preparation, public launch, managed context handoff, scoped session
capture, exact scripted reply, YAML/provenance validation, collection, cleanup,
manifest forwarding and its existing consumer. Include changed PATH/pin,
wrong/missing managed session root, stale context, absent selected login, mixed
concurrent callers and personal-marker controls. Preserve existing ambiguous,
reused and interrupted-turn rejection. These are focused tests before automated
gates, not a new Make target or permission for Developer-run gates.

Then the normal Stop check and outer live target exercise the affected existing
workflows: connected fresh/continued Shape-to-Commit, Build-only Reviewer rework,
native helper/profile auditing, semantic Review, cold offline proof and all five
shaping cases, plus the bounded test-owned managed compatibility runner. The whole
existing live target remains selected; untouched test files are not an exemption.

Challenge: an implementation could pass installer/unit/old compatibility checks
while the evaluation resumes personal Codex, misses isolated sessions, loses the
snapshot after terminal stop, or drops required artifacts before Review. Such an
implementation fails every-role-discovery, active-runtime-preservation and
verification-and-review. The updated scenario evidence explicitly covers this
combined route and retains existing negative controls.

The outer driver owns observations of ephemeral turns, actual native denial,
ordering, exact resumption, descendant identity and cleanup. Retain inspectable
concise receipts with concrete source/artifact bindings; never replace observations
with model claims. Independent Review assesses the contract, current Candidate,
owned gate receipts and retained artifacts, including source/test sufficiency.
It is not required to reconstruct private interactions from unavailable streams.
Required five-case evidence stays under its single existing manifest collector;
do not add a second target frame for managed compatibility. Existing normal gate
receipt/citation plumbing can retain concise compatibility proof. No product-side update runner is required. No change to
Build TargetEvidence, tracking authority or Review schema is required by this
reassessment.
