> Historical startup snapshot within the September 13 visit. The Shaper later
> directed continued reassessment; [current-main-reassessment.md](current-main-reassessment.md)
> supersedes the next-work/direction question below. No second visit was created.

# September 13 continuation startup

Visit started: 2026-09-13T16:00:55.892885Z. Source: current Shaper startup
instructions and read-only repository inspection. Current checkout is clean
main/2890d86f16c8819cfad12cbd98dbbdc8ee068b1c. Exactly one continuation entry
was appended. Original shaping, shaped_against, both prior visits, historical
reshaped_against and accepted decisions are preserved. No current approval,
baseline adoption, Build, implementation, stash operation or provider probe occurred.

## Existing outcome and accepted choices

A user explicitly installs Kogen's complete native runtime, authenticates natively
in a shared or explicitly selected project scope, and uses ordinary Shape/Build
commands. Missing selected setup stops with the appropriate command; no personal
installation/login fallback. Updates verify a staged candidate before activation;
active work retains its runtime, configuration and resumable state. Private model
discovery excludes personal context while root/helper tools and hooks retain caller
HOME/XDG semantics. REPAIR.md owns the selected named stdio mechanism and repair
acceptance. Twelve scenarios, four risks and check/live remain the contract; current
config still supplies two outer resumptions.

## Current-source discrepancies and evidence limits

- Original baseline is fdfaea11; last explicitly recorded reassessment is bf68fd70.
  Neither metadata block is replaced by this startup inspection. Current main adds
  file-based role context, diagnostics, isolated-process readiness, parallel live
  owners, required target-evidence retention and the five-case shaping evaluation.
- Current lib/kogen/harness.ex resolves the harness from KOGEN_HARNESS/PATH at
  launch. Managed Codex modules/tasks and mise.toml from the failed implementation
  are absent. This is a clean source baseline, not the previously probed Candidate.
- The retained denial-collector results report 17 focused offline passes and one
  212.1-second native compatibility success. Its prepare_probe.py requires two exact
  failed-Candidate source hashes; those source paths are absent here. The retained
  source is a partial prototype, not a self-contained current-main implementation.
  No credentials were inspected and current authenticated setup was not reverified.
  Preserve that historical success without rerunning it merely for a new receipt;
  do not treat results.json as current Build acceptance or complete reproducibility.
- test/support/shaping_evaluation/driver.py selects native sessions from personal
  ~/.codex/sessions; resume_transport.exp directly invokes PATH codex resume.
  These are concrete consumers affected by moving launch/runtime/session ownership.
  Reassessment must trace capture and exact fixture reply routing through managed
  state, preserving the five cases and source-bound evidence. This does not change
  public draft continuation into provider conversation resumption.
- scripts/check/README.md assigns ephemeral sequence audits to outer lifecycle
  owners. Independent Review assesses retained Candidate/receipts; it must not be
  asked to reconstruct private native interactions unavailable to it. Preserve the
  single required-evidence manifest and existing Build snapshot consumer. Existing
  test/** permissions cover fixture adaptation; no new generic evidence subsystem
  or Build policy is proposed.

## Outcome challenge and next work

A plausible implementation could pass the older compatibility fixture while the
new evaluation resumes personal PATH Codex or cannot find the isolated session.
That would fail the every-role discovery and active-runtime outcomes despite the
old fixture's success. Reassessment should map all current producer/consumer routes,
setup and retained evidence before recommending another Build. Historical prototype
checks alone cannot establish that combined route.

The Shaper supplied continuation startup but no new feature direction. Ask where
to continue, recommending current-main reassessment; record any eventual baseline
adoption only after an explicit shaping decision. Keep this directory unapproved.
Do not reopen settled engineering choices or promote parked same-build-refresh,
external-repository-cli, stopped-Build recovery or unrelated shaping work to backlog.

## Reading and validation

Read root README, all maintained draft prose/YAML and concise evidence, the retained
prototype source/tests/preparation script/results, linked completed preservation
contracts, shared execution guidance, the check workflow and shaping-evaluation
workflow. The historical archive remains historical, not a competing contract.
One configured read-only scout (gpt-5.6-luna/low) inspected current launch and new
consumer seams; root verified consequential PATH/session locators directly.
Python lacked PyYAML; use the available Ruby YAML reader for package structural
validation. No dependency installation or aggregate verification gate is needed
for this startup-only documentation save.
