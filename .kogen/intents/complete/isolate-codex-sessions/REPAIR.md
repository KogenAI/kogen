Current amendment: [Build 9TbQ failures and next repairs](evidence/build-failure-9TbQ-2026-09-14.md).
It supersedes earlier latest-failure labels; preserve all existing requirements.

# Current repair contract

Read this file and INTENT.md as the current contract. [Earlier repair chronology](REPAIR-history.md)
and dated audits retain observations, not competing instructions about what remains.
Scope, twelve scenarios, four risks, approval provenance and gate ownership are unchanged.

## Verified shaping findings

The complete failure analysis is [September 14 audit](evidence/build-audit-2026-09-14.md).
The [native helper capability proof](evidence/helper-capability-2026-09-14.md) resolves
its previously open runtime capability question. The exact pin supports required
kinds when valid standalone definitions are loaded. Preserve built-in semantics.
Hook dependency and midnight-correlation defects have source-bound reproductions
and disconfirming controls. Compatibility evidence distinguishes hostile-positive
from safe-negative observations. Nested rework acceptance does not prove the
fixture's required ordered sequence.

## Six bounded integration obligations before acceptance

1. **Complete hook artifact:** materialize every dependency of copied hooks. Run
   the actual fixture-local hook with managed Shaper/Reviewer restoration enabled;
   it must return without Check or mutation. Retain Developer restoration/Check
   controls. No timeout increase or disabling hooks.
2. **Native event correlation:** recognize runtime environment updates using
   supported native structure while preserving exact submitted message, turn and
   terminal identity. Replay the retained midnight case; reject genuine intervening
   user turns, forged updates, duplicates and reordered/misbound terminals. Do not
   use an arbitrary text-prefix filter. Emit actionable invalid-correlation evidence
   instead of waiting a full deadline for a permanently invalid sequence.
3. **Agent definition discovery:** generate valid Kogen-owned standalone definitions
   in managed discovery, without overriding required built-in semantics or writing
   user project files. Verify actual loaded definitions, native argument fields,
   child roles/profiles and completion. Test missing/unrelated definitions and
   existing-file refusal. Prove regeneration and overlapping operations cannot
   rewrite active configuration or affect another selected account/runtime.
4. **Compatibility evidence meaning:** retain separately labeled hostile-positive
   and isolated-negative receipts bound to their actual executions. Give independent
   Review enough context to interpret them, with controls where an actually unsafe
   isolated run fails. Never prescribe an accepting verdict.
5. **Controlled rework:** the intentional first omission must cite only existing
   evidence and reach the first Reviewer without a malformed-handoff resumption.
   Preserve exactly one Reviewer-directed same-Developer rework and fresh acceptance
   for this fixture; preserve invalid-reference controls and the configured budget.

6. **Reconnect classification:** [Latest observed completion](evidence/reconnect-classification-2026-09-14.md) shows a resumed native turn completing after a reconnect warning while Harness rejects the earlier error. Distinguish recoverable stream notifications from terminal failures using the actual parser and positive/negative controls; retain all session, exit, handoff and gate requirements. No blind retries or accepting incomplete streams.

## Combined preservation checks

These are integration requirements, not additional product features. Existing
role-restoration and selected-account fixes must be exercised through public
fresh/continued Shape, Build and Review consumers. Login preflight must not consume
final launch receipts. Preserve exact runtime/account/session across resume,
shared/project selection, concurrent generations, failure and cleanup. Use focused
non-gate tests with the production operation route, not an override that bypasses
managed setup. Existing source/probe success does not establish combined acceptance.

First run deterministic focused regressions for these boundaries. Then the Developer
Stop owns Check; the outer Build owns the unchanged declared live targets and fresh
independent Review. Retain complete actionable case-level failures and current
source-bound evidence. Do not add retries, extra resumptions, skipped assertions,
provider recovery, new commands or a global upgrade workflow. Preserve the successful
mechanism and current fixture conventions rather than restoring old prototypes.

## Evidence limits

Shaping has established diagnoses and helper capability, not implemented repairs.
Production placement of generated agent definitions, repaired consumer integration
and all nine live cases together remain unverified. macOS x64 execution remains an
explicit host coverage limitation. External provider availability cannot be proven
permanently or guaranteed by more shaping probes. No claim of zero future failures
or approval readiness substitutes for these acceptance results.
