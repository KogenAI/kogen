# Probe, 2026-09-26, MacBook Pro (M4 Pro), detached worktree at 7ed41f66

Toolchain: python 3.14.7, elixir 1.20.2-otp-29, erlang 29.0.3 (mise, activated explicitly in the
non-interactive ssh shell: `PATH=$HOME/.local/bin:$PATH; eval "$(mise activate bash)"`).

1. `MIX_ENV=test mix test test/kogen/candidate_verification_test.exs test/kogen/verification_reuse_test.exs`
   - KOGEN_LIVE_LOG_DIR unset: exit 0, 0 failures, 20 s.
   - KOGEN_LIVE_LOG_DIR=/tmp/probe-outer-live: exit 2, 7 failures, 15 s.
2. `KOGEN_LIVE_LOG_DIR=/tmp/probe-outer-live2 make check` (full offline suite): exit 2 after 180 s
   (mix test stage 177 s). Exactly these 7 failures, and no others:
   - Kogen.CandidateVerificationTest:
     - a marker a target writes under KOGEN_LIVE_LOG_DIR survives publication … and a caller-set KOGEN_LIVE_LOG_DIR wins
     - the controller runs every target through VerificationRunner with cwd equal to the Candidate
     - the verification child drops inherited MIX_BUILD_PATH, MIX_DEPS_PATH and MIX_EXS …
     - the verification child's environment lacks KOGEN_WRITE_BOUNDARY even when the controller's own environment sets it (:249)
   - Kogen.VerificationReuseTest:
     - any changed byte forces every target to run fresh, even after a passed cycle
     - cycle 1 fails at b; a byte-identical Candidate's cycle 2 runs check fresh, reuses a, and runs only b
     - a new outer attempt reuses nothing even on the identical Candidate

Mechanism: the fixture Makefiles write to `$KOGEN_LIVE_LOG_DIR`
(candidate_verification_test.exs:81-85, verification_cycle_fixture.ex:189-195), and
VerificationRunner.live_log_dir/1 (verification_runner.ex:112-120) keeps a non-empty caller value,
so under a controller the markers go to the outer directory and the tests read an empty
control live-evidence.
