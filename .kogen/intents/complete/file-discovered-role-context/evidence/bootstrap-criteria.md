# Bounded accepted-engine proof criteria

Frozen before execution on accepted predecessor 1843dcfaf8b0b7c9d9f91d5c3e8ca92f41633fcb. Both isolated copies originate from git archive of that commit; each owns copied deps/build outputs. No production edits or paid providers.

Reconciles historical bf68fd70 criteria: retain C1 real full offline Stop gate, C2 invalid first handoff and exact resume, C3 fresh Review and publication, C4 loaded accepted engine remains distinct from freshly compiled prototype, C5 prototype passes the full existing offline suite with adapted inline-context consumers, C6 no unrelated slice changes. The prior literal prohibition on execution-policy placeholders is obsolete: this baseline already accepted shared policy and it must remain unchanged. Do not relabel the historical 182/183 result.

The tiny synthetic Approved fixture links requirement.json requiring probe-value.txt to contain ready. The fake provider reads actual on-disk record fields and linked requirement/current source. First handoff omits its scenario. Second handoff is valid but source is stale; a fresh Reviewer must request rework. Third Developer repairs that source and a new Reviewer must accept. Every Developer invocation runs the real tracked Stop hook and full make check; two exact-session outer resumptions maximum. Record attempts, source-derived observations, gate receipts, provider argv and loaded/on-disk module identities. Retain failures and corrections.

Then execute the freshly compiled prototype through a second isolated fake-provider lifecycle to establish real locator dispatch, retaining prompt packets. Assert actual locators bind to current token/record, no inline contract marker, source-derived rework and final publication. No parent-supplied verdict or omitted file payload. Existing existing suite controls cover public Shape routes; further final implementation scenarios remain Developer-owned.

Scope limit: a disposable prototype establishes feasibility and gate composition, not final implementation acceptance or model judgment. No paid live suite, model change, storage migration or production commit is authorized by this probe.
