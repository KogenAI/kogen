# Readiness proof result

Passed on accepted predecessor `1843dcfaf8b0b7c9d9f91d5c3e8ca92f41633fcb`. Machine-readable results: [proof-result.json](proof-result.json). Frozen method and limits: [bootstrap-criteria.md](bootstrap-criteria.md). Reproduction: [README](README.md).

## Observed

- Accepted-engine and freshly compiled prototype each completed the real Build driver, three real full Stop checks, invalid first handoff, two exact `probe-developer` resumptions, fresh source-derived rework/accept Reviews, and actual disposable publication commits. The final full gate reported 199 tests passed, 6 live tests excluded; all six owned Check receipts passed.
- Accepted engine retained its original loaded module identity after Stop compiled the prototype to disk. The next phase loaded and retained the new prototype identity. No mixed-engine assumption remains unresolved for this baseline.
- Actual fresh/continued Shape dispatch stayed at 11,777/13,967 bytes when read input grew from 22 to 990,000 bytes (identity/time normalized for equality). These routes already avoid task-body embedding.
- Each Build retained 1,215,000 bytes of synthetic evidence, plus earlier/current source snapshots. The accepted inline engine's final Reviewer prompt was 9,986,641 bytes. Prototype Reviewer prompts were both 11,855 bytes; Developer rework prompts were 1,076 and 1,074 bytes (different failure categories). These are fixture transport sizes, not model quality, token billing or general performance estimates.
- The actual fake provider held current source at `ready` while changing only the linked requirement from `ready` to `different`: verdict changed from accept to rework. This separate oracle control confirms file dependence, complementing actual driver stale-source rejection/repair/publication.
- The actual prototype fixture reader rejects a stale attempt, wrong Candidate, missing record and malformed record. Existing full-suite mutation, retention, publication and session controls passed. Complete snapshots preserve the earlier stale source and later repaired source independently.

## Preserved failures and corrections

Historical bf68fd70 extraction remains 182/183, not a retroactive pass. Its assertion required settlement detail inline. Current prototype assertions instead check complete persisted settlement detail and concise prompt location; no coverage is removed. The old criterion excluding shared execution-policy placeholders is inapplicable because the current accepted predecessor already contains that policy; it was preserved byte-for-byte.

Initial helper prototype inspection found that it discarded detailed persisted failure text and silently tolerated unreadable locator records. Root required corrections before the full proof. The initial four focused passing tests did not establish those properties; corrected focused checks passed 18 tests, followed by the full real gates. No whole-driver failure was retried. First-phase Stop responses and all owned Check receipts are retained; its first two full textual check logs were overwritten by existing Stop-log behavior, while the final full log was copied. Later probe code additionally copies each full log; that logging-only adjustment did not change criteria or fake verdict behavior.

## Limits and final Build obligations

This is a disposable feasibility prototype, not an accepted product implementation. It establishes actual dispatch/file access and deterministic lifecycle composition; it does not evaluate general model judgment. Its test helper retains a legacy inline-parser branch for preexisting direct fixture tests, not production dispatch. The real Developer must implement and gate all selected scenarios, including complete normative reading coverage (direct or delegated), all five actual launch routes, source-dependent negatives and preserved history. The prototype patch is advisory and need not be copied.

Raw synthetic prompts, complete records, provider arguments, observations and hash inventory are retained at `.kogen/runtime/shaping-evidence/file-discovered-role-context/bootstrap-20260913-bng81d7u/`. Editable scripts and the prototype patch remain here; temporary dependency/build copies stay outside the source repository. No production source, branch, approval or commit was changed by these probes.
