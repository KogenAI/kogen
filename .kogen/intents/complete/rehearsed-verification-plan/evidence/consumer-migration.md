# Maintained `live` consumer migration inventory

Source inspection at shaped HEAD found these maintained consumers of the retiring target name. The Developer must rerun the inventory after implementation; historical Complete packages and retained receipts remain exact historical bytes.

- `Makefile`: remove `live`; add `live-shape-to-build`, `live-reviewer-rework`, and `live-general`; preserve the other three boundaries. No alias recipe.
- `README.md`: replace generic Developer prohibition and verification-selection/run guidance with declared-target language and the six exact boundaries. Explain explicit catalog expansion, never a broad command.
- `scripts/check/README.md`: replace the configured-default `live` contract, prerequisites, and bootstrap prose with exact owner boundaries.
- `workflows/codex-runtime-upgrade.md`: replace check/live ownership with the exact runtime-relevant catalog targets.
- `priv/kogen/prompts/developer.md`: remove hard-coded `make live`; render the exact readiness commands and describe ordinary cited results without inventing controller-observed readiness records.
- `priv/kogen/prompts/shaping.md`: replace the `[check, live]` example and add the required proof schema, causal selection rules, affected assertion/fixture paths, and aggregate prohibition.
- `lib/kogen/verification_policy.ex`: retain `live` only as the immutable current hook's installing-protocol bootstrap denial token; deny `check` plus every catalog target, while prompt text never renders nonexistent `make live`. The next verification-authority cutover removes the token with the old Stop protocol; no generic compatibility or fallback path is permitted.
- `lib/kogen/codex/compatibility.ex`: its disposable Developer-denial fixture must use the exact target(s) required by that fixture, not retired `live`.
- `test/kogen/live_shape_to_build_test.exs`: retain only the shape-to-Build owner; move `Kogen.LiveReviewerReworkTest` and its exclusive support into a separately selected owner file.
- `test/kogen/live_test.exs`: migrate/rename as the `live-general` owner if useful; the Make recipe selects it explicitly.
- `test/kogen/selective_verification_targets_test.exs`: update the source-derived owner matrix, expected case count, absence-of-aggregate controls, prerequisites, and behavior guide.
- `test/kogen/live_scheduling_test.exs` and `test/support/shape_to_build_probe.exp`: migrate co-located-owner assumptions and generated scenario proof data.
- `test/support/shaping_evaluation/README.md`, `driver.py`, and maintained fixture facts: migrate production target claims; classify synthetic fixture-local names explicitly. `scripts/check/development-evidence.md` remains labelled historical evidence.
- Maintained offline lifecycle, verification-policy, scenario-contract, Shape-task, Shaping-evaluation, Stop, and compatibility fixtures that seed `live` must select the exact boundary represented by their behavior. Generic fixture-only target names may remain when they are clearly local synthetic names and are not maintained Kogen target selectors; tests must distinguish these from leaked production aliases.

The final offline inventory searches maintained source, tests, prompts, scripts, and documentation for semantic consumers of `live`. Mere `:live` ExUnit tags and historical prose/receipts are not Make aliases; broad tag-only Make selection is forbidden.
