# Shaping investigation

Baseline: main at 6072c0f550183a3dd6c5153e97154ec10653df27, clean tracked working tree at startup. README, config, Makefile and maintained check workflow read. Config permits two outer resumptions; Make declares check and live. Current history already includes file-discovered context, actionable errors, reliable isolation and parallel live cases; the provisional serial index is not a current acceptance ledger.

Observed source: test/support/isolated_case.ex run!/3 discards successful captured output; run/3 retains it. lib/kogen/build.ex run_declared_targets/5 and target_receipt/6 capture output then retain only a 4,000-byte tail; there is no TargetEvidence module in this baseline. snapshot_references/2 retains cited files. Build.Tracking maintains separate Developer and Reviewer snapshots and existing controller integrity protections.

Ran [probe_output_boundary.py](probe_output_boundary.py) from the repository root using mix test and the actual isolation helper/supervisor. [Result](output-boundary.txt): one passing observation test; the marker is present in run/3 output and absent from successful run!/3 forwarding. The test uses two fresh isolated child executions to compare those entry points. It creates owned temporary probe files and removes them. No gate or provider was invoked. This is evidence of the current defect, not a passing end-to-end implementation proof; the required public-driver regression remains Build work. The marker payload is a diagnostic sentinel, not a valid proposed manifest.

A configured read-only scout (gpt-5.6-luna, low; native explorer) traced the advisory partition and decision inventory. Root inspected the historical target-evidence contract and the inventory's conflicting retention amendment. The old contract mandates all-file Reviewer citations; the later inventory explicitly lists automatic uncited-artifact retention as pending H-01. The newer user-supplied follow-up selects that retention approach; the user corrected a redundant request to reconfirm it. This Draft follows that brief, while final Intent approval remains separate. The historical live-manifest-protocol proof directory referenced by that contract is absent; no passing historical integration receipt is claimed or reused.

Transferred scope: strict optional manifest protocol, extraction before truncation, actual isolated boundary, full public consumer proof, mechanical retention separated from semantic citations, negative controls and immutable provenance. Deferred scope: future shaping evaluation cases and frozen continuation seed, metrics and broader private archival work. Preserve their original owners; this Draft does not mark those obligations complete.
