# Composed policy v7 result

V7 completed an **accepted Build** in 484.80 seconds with four native root invocations. Strict postflight passed: Stop failed then passed in the exact Developer session; the initial fresh Reviewer returned the controlled notes finding; the exact resumed Developer addressed it; a second distinct fresh Reviewer accepted; the withheld semantic oracle passed; Complete publication exists; Git is clean; profiles, usage, and cleanup are valid.

This quality integration run could overlap separately owned Shaping probes by predeclared protocol. Elapsed time and cost are descriptive and support no causal timing ranking. V6 remains preserved as a failure.

## Intervention result

The fresh baseline ignored `__pycache__/`, `*.pyc`, and `*.pyo`. Five helper spawn attempts all used supported native kinds and configured profiles: three Luna-low `explorer` scouts and two Luna-medium `worker` helpers. No invalid `scout` or `expert` native kind was attempted. Descendant audit passed.

Developer authored every candidate edit with Python, including implementation, recovery token, and resumed notes. No `apply_patch` authored edit appears. Developer verification created and removed cache files, but those ignored files did not alter Candidate identity. Reviewer roots and helpers made no Git-visible Candidate change; the immutable Review gate passed. The frozen baseline had no ignored cache sentinel, and the Developer removed its own generated cache before Candidate capture. Preservation of a preexisting ignored cache during Review was therefore unobserved rather than proven.

Developer handoffs used concrete implementation and verification sources and did not cite or recursively snapshot scenario-tracking `record.json`. The published tracking record is 104,109 bytes; v7 did not reproduce v5's large recursive growth.

## Evidence

- Strict postflight (sanitized gate receipt): `strict-postflight-summary.json` (the raw `arm/evidence/postflight.json` remains private because it contains native helper payloads)
- Descendants and native kinds: `arm/evidence/descendant-audit.json`
- Cleanup and elapsed receipt: `arm/evidence/arm-receipt.json`
- Frozen prompt delta: `arm/evidence/prompt-delta.json`
- Source-only accepted receipt and Complete hashes: `accepted-golden.json`
- Source-only accepted implementation: `accepted-source/release_manifest.py` (SHA-256 `5e93b5f6e3b4f8074af8a186eb14ea5a28d1fbf722688a8858770255323096c1`)

Complete output hashes cover 5 files. `accepted-golden.json` makes acceptance reviewable without retaining the disposable repository in curated publication evidence.

## Economics

Complete execution API estimate: **$1.18818472**: roots **$1.0947024**, helpers **$0.09348232**. Resume counters were delta-adjusted; reasoning was not double-counted. This is an API estimate, not an invoice.
