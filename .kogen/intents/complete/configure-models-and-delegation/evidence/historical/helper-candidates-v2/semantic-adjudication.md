# Root semantic assessment

## Corrected Scout pair

Root read both complete `runs-corrected/scout-*/answer.txt` outputs and the cited
authorization contract, implementation and decision evidence. Both correctly
distinguish accepted behavior from current defects across all seven predicates:
ASCII header names, trimmed values, duplicate denial, current dictionary collapse,
current legacy bypass, the rejected dictionary approach, and irrelevant INC-51.
Neither recommends the rejected fix or mistakes the unrelated incident for evidence.

Citation fidelity is imperfect. Terra's boundary-trim citation quotes a sentence
that spans decision 001 lines 8–9 as though it were contained on line 8. Luna
reconstructs several quoted sentences across wrapped lines and removes literal
Markdown punctuation; for example contract lines 7–8 become one quotation attached
only to line 8. These are not verbatim per-line quotations. Both also provide
accurate nearby contract/code evidence for the substantive conclusions.

Thus both pass factual interpretation, but neither gets an unqualified strict
exact-citation pass. Preserve the automatic citation failures alongside this
meaning-based assessment rather than deleting or treating them as factual errors.
This case supports retaining Luna as a bounded factual scout candidate; it does
not demonstrate that every returned quotation can be trusted verbatim or that
Terra is categorically better. Native identity, root verbatim return and complete
cost/time are separately established by the corrected campaign postflight.

`cache-attribution.md` subsequently establishes an additional Luna route failure:
its native non-`-B` Python imports created bytecode during the read-only task.
Terra did not create that cache. Thus the factual result does not constitute an
unqualified route-compliance pass for Luna.

The original self-report-gate attempts are excluded from the matched route outcome;
their complete child answers remain exploratory evidence and research expenditure.

## Worker

The independent executable oracle passes both Terra-medium and Luna-medium across
normalization, intervals, merge semantics and integration. Both fixture Git states
contain only the permitted `merge.py` change. This is a substantive matched task
pass, not an inference from exit status or a helper's completion claim. No root
source rewrite was needed. Later postflight found ignored bytecode caches, but
`cache-attribution.md` establishes that both were created by the external oracle
after native work ended. Both workers used `python3 -B`; the oracle did not.
Those caches are not helper violations. The initial postflight inference is
superseded, preserved with timestamps/commands and corrected classification.

## Expert

Root read both complete corrected answers and the supplied adapter, contract and
wire events. Sol-medium and Astra-medium both correctly reject the measurement,
identify cumulative-versus-last input, the wrong compaction event paths/count and
timestamps, and the inverted cleanup predicate. Both prescribe the corresponding
minimal repairs and discriminating tests, preserve independent schema/output
validation and valid zero-compaction observations, and invent no profile failure.
Their nonstandard rejection strings mean rejection; an enum-only screen cannot
turn that ordinary meaning into acceptance or a missed diagnosis.

Astra is more precise about execution order: the supplied fixture raises at line 5,
so the line-3 wrong value and cleanup-branch outcomes are intermediate/conditional
counterexamples, not successful returned summaries from the unchanged fixture.
Sol correctly identifies that assertion in its compaction finding but does not
repeat the qualification in its other counterexamples. Astra also supplies a
specific incomplete-lifecycle negative test. Preserve these quality differences;
do not report the answers as identical.

Both pass the supplied-category diagnosis and repair task. The prompt names the
three suspected defect categories, so this is not blind discovery and cannot
establish general expert reliability. Model/effort, orchestration and complete
economics remain subject to native postflight; self-reported identity is irrelevant.
