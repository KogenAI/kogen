# Configure role models and unify delegation guidance

Approved for one Build. Start with [intent.yaml](intent.yaml), [scenarios.yaml](scenarios.yaml), [policy.md](policy.md), [risks.yaml](risks.yaml), and [questions.md](questions.md). [decisions.md](decisions.md) owns scope/provenance; [evidence/README.md](evidence/README.md) navigates selected historical support. Historical reports are evidence, not additional implementation requirements.

The human confirmed the model/delegation slice and `check` + `live` on 2026-09-12. The selected table in intent.yaml carries forward the previously accepted six-profile choice. This did not change the active Shaping conversation's launch profile. Final explicit approval is recorded in approval.md.

The Build applies the selected config, factors shared execution/delegation guidance and profile rendering across all four role routes, preserves native errors at the existing Reviewer boundary, and verifies configuration plus real root/helper dispatch. It retains one Developer conversation and two outer resumptions. Existing role-context architecture is the baseline; earlier serial proposals have not landed and are not prerequisites to import.

Verification uses existing Make targets. Offline public-route controls establish deterministic configuration/authority behavior. Existing live lifecycle proof plus a compact native dispatch fixture establishes current integration, not general model quality, delegation effectiveness or cost savings. Keep the dispatch fixture limited to profile transport and concrete deterministic facts; do not import the five-case Shaping evaluation or broader optimization harness.

Approved ordinary source, test-fixture and evidence ownership is described in risks.yaml. No separate installer, migration or ownership-transfer feature is proposed. No production code is implemented during Shaping. The human explicitly approved this package in this conversation; see [approval.md](approval.md).

Known verification maintenance belongs to this small Build: update existing assertions of the old profile defaults in intent_test.exs and lifecycle_test.exs. The semantic live fixture also needs its already-required invalid-configuration behavior repaired: require exactly three arguments in its synthetic bin/route instead of recognizing one magic fourth argument, and cover generic extra/missing arguments and invalid role/model/effort. This repairs existing verification of model routing; it adds no production CLI behavior or broader suite optimization. The stopped shaping probe identified this defect, and a corrected isolated fixture passed the complete offline gate. Its corrected live Review was not run.

Shaping is complete for this package. The full live attempt was stopped at the human's direction and is not passing acceptance evidence. Future implementation and Build-owned check/live remain required. Do not run a Build or replay these probe campaigns during Shaping.
