# Current baseline reconciliation

Observed on `main` at `c1f085324f78d5030c8d3d7b6efc2df248ff01c2`.

- The delta from `7c7c3426` is the completed `harden-whole-test-suite` Build. It installs structured offline receipts, strict shared Make inventory, provider-outcome normalization, isolated completion receipts, current live fixtures, and a 312-declaration source-bound reliability catalog.
- `.codex/hooks/**` and `.codex/hooks.json` did not change. Current `stop_runner.py` derives the launched Git root and checks the verification context project root, so explicit Candidate launch cwd plus an explicit Candidate verification root can preserve hook bytes.
- The stale package named three focused workspace tests that do not exist at HEAD. They are planned tests, not existing proof.
- Current targets are exactly `check`, `cold-offline`, `live-general`, `live-shaping-quality`, `live-native`, `live-reviewer-rework`, and `live-shape-to-build`. This Draft selects only cold-offline and the three live boundaries causally affected by workspace routing.
- Failed records `VZFIeCIgbbYcNE4VRr3TcYUy` and `1vg5QEsjup-8dCzHbvkuGXBZ` are historical. They converged on a fake Developer correctly writing a dangling Complete entry in Candidate while a historical assertion looked in control.
- The inner JSON receipt from `scripts/check/offline.py` must be retained or content-bound into controller tracking separately from the outer `make check` receipt before successful Candidate cleanup.

No paid target was run during reshaping. Existing provider probes are historical evidence, not fresh readiness proof.
