READ-ONLY AUDIT. Do not modify, create or delete any file. Do not run git commands that write, do not run mix compile, mix test or make. Only read files and run read-only commands (cat, sed, grep, rg, ls, git show, git log, git diff).

You audit the Kogen Intent package at .kogen/intents/drafts/isolated-candidate-workspace/ (intent.yaml, INTENT.md, scenarios.yaml, risks.yaml, questions.md, approval.md, references.yaml) in this checkout, which is main at 363c20af. Kogen's quality bar and package format are priv/kogen/prompts/shaping.md. Assume these land before this Build, in order: shaping-preflight-audit (package at .kogen/intents/approved/shaping-preflight-audit is NOT in this clone; it adds lib/kogen/shaping_audit/**, mix kogen.audit, VerificationPlan.proof_errors/4, VerificationPlan.frozen_paths/0 (today the catalog), VerificationPolicy.controller_paths/0 (hooks.json, verification_policy.py, check.sh, environment.py, stop_runner.py), a Shaping-only Stop hook and auditor launch, edits live_shape_to_build_test.exs and the probe, and adds NO Build admission gate), then fortify-paid-verification (staged package: .kogen/intents/drafts/fortify-paid-verification in this clone; controller-owned verification, Stop scripts inert without a v1 context, preflight requires only verification_policy.py and the PreToolUse registration, Kogen.LiveTrackingRetention.preserve!/3 for the Shape fixture). The Build runs under main's controller at that time with --route claude-dominant-adversarial-codex (Claude Developer, Codex Reviewer/Expert); .kogen/config.yaml keeps default_route: claude.

Find, with file:line evidence at HEAD:
1. Infeasible or self-contradicting scenarios, including contradictions with the code at HEAD (anchors, function names, arities, file layouts cited in the package).
2. For each scenario, a plausible wrong implementation that passes its named proofs (proof.offline selectors plus the paid target).
3. Missing guarded paths (may_change_guarded_paths vs every file the work must touch), wrong or nonexistent selectors.
4. Conflicts with the controller that will run this Build: catalog byte-freeze (priv/kogen/verification_targets.yaml, Makefile), the Stop and PreToolUse scripts and registrations it reads, VerificationPolicy.preflight, role prompts rendered from the Candidate, Contract.verdict exact keys, rehearsal trace identities (scripts/check/rehearsals.exs), priv/kogen/test-reliability.yaml source hashes.
5. Unnecessary paid targets, or an edited live-test owner (catalog owner in priv/kogen/verification_targets.yaml) whose target is not selected.
6. Scope creep beyond ROADMAP row: a worktree, harness home and credentials per Build; Shaping keeps working during Builds; one lifecycle target (live-shape-to-build).
7. The 2026-09-25 Build-failure lessons: (a) every new or changed persisted artifact (record, sidecar, packet, receipt, manifest, owner record) traced through all consumers, including live-fixture retention, copying, resolution after cleanup (Kogen.Build.Evidence.resolve/2, test/support/review_packet_audit.ex, test/support/live_rework_audit.ex, test/kogen/live_shape_to_build_test.exs, test/support/live_reviewer_rework_fixture.ex); (b) new code reading priv/ or app files at runtime inside tests that run from an isolated code copy; (c) live fixture wiring (route, profiles, login scopes, preflights) for every selected live target; (d) fixture or workspace copies that inherit controller volatile state (.kogen/build.lock, .kogen/runtime); (e) "no plumbing without a caller": list everything the Intent adds (function, module, config key, role, field, option) and name its caller and proof in this Intent; any item without one is blocking; (f) "no unrequested splits": any requested scope deferred to another Intent is blocking unless the package records the Shaper's decision.

Output: a numbered list of findings, each with severity blocking or advisory, the scenario id, the evidence (file:line), and a concrete fix. Then a one-paragraph verdict. Be concise.
