READ-ONLY AUDIT. Do not modify, create, delete or move any file. Do not run make, mix test, mix compile or any build or test command. Only read files (cat, sed, grep, git show, git log) and answer.

You are auditing a Kogen Intent package before it is built. Repository: the current directory (a clone of Kogen main at 363c20af). Package: `.kogen/intents/drafts/isolated-candidate-workspace/` (INTENT.md, scenarios.yaml, risks.yaml, questions.md, intent.yaml, approval.md, decisions.md, evidence/write-boundary-probe-2026-09-25.md and evidence/write-boundary-probe/*).

Context: the Intent was already shaped (Candidate worktree, per-Build harness home, credential binding, publication). The human Shaper then decided "Keep it in #5": BLD-12's launch-time write boundary must be delivered in this same Intent. Every Build role and helper must be physically unable to write outside its Build's worktree, enforced by macOS. This revision adds INTENT.md outcome 13, the scenarios `role-write-boundary`, `write-boundary-fails-closed` and `codex-roles-inside-boundary`, new risks, questions.md R5/R8-R10, and selects the existing paid target `live-reviewer-rework` next to `live-shape-to-build`. The mechanism is a Build-wide `sandbox-exec -p` Seatbelt profile, allow-default except file writes. Main's controller will run the installing Build (self-hosting, DIRECTION D9: no changes to Makefile, `priv/kogen/verification_targets.yaml`, hooks, Claude settings, prompts or `.kogen/config.yaml`). Intents `shaping-preflight-audit` and `fortify-paid-verification` are assumed landed before this one: `plan/staging` is not in this clone. Assume #4 makes the parent controller run `make check` and the targets itself, outside role process trees.

Focus on the write-boundary additions. Report, each with file:line evidence:
1. Infeasible or self-contradicting scenarios or outcome text, including contradictions with the earlier outcomes 1-12 (for example fake-harness receipts, shaping-during-build, the Expert launched by `mix kogen.expert` from the Developer's tree, readiness, the live fixtures' retention).
2. A plausible wrong implementation that would pass the named proofs (proof.offline selectors plus the paid targets) but miss the outcome.
3. Missing guarded paths (`may_change_guarded_paths` in intent.yaml) or affected_paths for files the design must touch; nonexistent selectors that are not listed as created by the Developer.
4. Conflicts with main's controller at 363c20af: the catalog freeze, the Stop scripts, `VerificationPolicy.preflight`, settings.json passed by path, `--setting-sources project` (would anything added here load into the installing Build's own roles?), and anything the running controller reads from disk.
5. Real macOS or harness facts the design gets wrong: Seatbelt semantics, nested sandbox_apply, canonical paths, keychain writes, Claude Code 2.1.281 temp dirs, Codex 0.156.1 CODEX_HOME writes, setuid exec, LaunchServices, Apple Events. Also any escape route from inside the profile that the design misses and that writes outside the grants (launchd, XPC services, `open`, osascript, mounting, a hardlinked or renamed ancestor, `git` in control through a grant, the per-Build temp dir, `KOGEN_RAW_LOG_DIR` placed inside control).
6. Whether the second paid target (`live-reviewer-rework`) is justified under DIRECTION D8's provider-only rule, or unnecessary. Also whether any other scenario needs a paid target it lacks.
7. Scope creep beyond "Build roles cannot write outside their worktree", or plumbing without a caller (every added module, function, env var, record field, test tag or file must have a caller in this Intent, exercised by its proof).
8. Build-failure risks from the lessons in the package: especially existing fixtures whose fake roles write outside the Candidate (the 2026-09-19 attempt failed 47 tests), and the inherited mode.

Classify each finding as blocking or advisory, and propose the minimal fix. Be concrete and brief. No preamble.
