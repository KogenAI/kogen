# Amendment evidence and recovery receipt

## Preservation

Original Approved bytes and failed Candidate inputs were frozen under the failed
Build's diagnostic amendment directory. evidence/recovery-inputs contains the exact
tracked patch, untracked source archive and hashes. No production source, original
Approved file or controller record was changed. The amendment Draft preserves identity
but is pending new approval. Failed evidence is not converted into a passing receipt.

## Input delivery and chronology

Root ran the evidence-local delivery_probe_test.exs with mix test: 1 passed, seed
114869, measured case approximately 1.995 seconds. It invokes the actual public Shape
route through CompiledFixture with a fake executable only at the provider boundary.
The fake observes the ordinary fixture README request on its first invocation;
missing request exits 42. The rendered policy does not contain the test request.
This proves first-invocation file availability without API/production changes, not
that a real model reads it or that the future driver is correct. That remains part
of Developer-owned rehearsal and real live proof.

The retained real middle-attempt CSV transcript puts the approval question before
the save-without-approval instruction. amendment-probes/delivery/chronology.json records
those times and the unsupported retroactive-instruction claim, not a general verdict
on the question. The amended driver must capture the current request before the first
substantive turn and grade only then-available facts.

## Cleanup experiment and limitations

Configured worker gpt-5.6-luna / medium used existing IsolatedCase APIs in evidence-only
sources. Its baseline focused owner test passed 4/4 in isolation. Initial probe autorun
and later root review identified validity issues: unintended ExUnit autorun, readiness
before its dependent marker, and unequal arm delays. Original source/observations and
corrections remain in amendment-probes/cleanup/RESULTS.md.

Corrected comparison uses equal 3500ms startup delay, 2000ms collection timeout, and
7000ms readiness startup bound. Without readiness: timeout, cleanup diagnostic and
absent marker. With readiness after marker: timeout, cleanup diagnostic and present
marker. The existing readiness API can express the needed test contract; this is not
proof of the historical scheduling cause or all descendant assertions. The production
repair must use the real PID/root prerequisites and retain actual termination checks.

## Clean recovery preparation

Prepared /Users/almirsarajcic/Projects/AppBuilder/kogen-compact-shaping-repair on branch
repair/compact-shaping-quality at accepted 56af370a. The original dirty worktree is
untouched. Installed dependency sources were copied privately using the repository's
DependencyFixture helper, without sharing writable build caches or fetching packages.

A tracked-patch dry-run was first accidentally invoked in the already-modified original
checkout and failed as expected for a patch already represented there. No files were
changed by that dry-run. Corrected execution in the intended clean repair worktree
passed. This is not an attempt to rerun a failed Build or relax its preconditions.
The untracked archive paths and bytes match the preservation inventory.

After amendment approval, synchronize this package into the repair worktree's ignored
Approved directory. Build starts clean and its new Developer imports preserved source
inputs, then repairs them within the amended paths. Old Build budget/evidence stay
frozen. No unaccepted bootstrap commit, gate bypass or editing of a running controller.

No full corrected production rehearsal, live campaign or new Build was run during this
amendment. The new Developer's gates and fresh independent Review remain required.

Recovery-worktree setup verification: mix compile --warnings-as-errors exited 0.
Dependency compilation emitted yamerl deprecation warnings; Kogen compiled successfully.
This is bootstrap verification of accepted code, not a gate receipt for the amendment.
