# Reassessment after accepted 06, 2026-09-13

The Shaper explicitly reported 06 done and asked to continue 08 and reread it. README,
updated note 08, current config and accepted source were inspected. HEAD is
56af370a39719ed6aca378c1974bea918800049d; tracked worktree was clean. Original shaping
identity and provenance are unchanged. This is the same conversation, not a fresh visit.

The configured Luna-low scout inspected accepted 06. Root verified the consequential
one-frame constraint against lib/kogen/build/target_evidence.ex: duplicate frames fail.
The integration therefore has one collector for five concurrent cases. Existing 06
owns generic strict-frame/path/hash/uncited-retention and publication controls. This
Intent owns producing and delivering the new case artifacts. No engine work is needed.

## Bounded real composition probe

Uncertainty: can five simultaneous producers use the actual isolated compiled-YAML
context and leave one valid manifest whose artifacts survive supervisor cleanup and
reach the real target evidence consumer? This affects the proposed collector design.

Ran: mix test .kogen/intents/drafts/compact-shaping-quality/evidence/aggregation_probe_test.exs

Observed: 1 passed, about 0.972 seconds for the measured ExUnit test, seed 238664.
Five Task producers reached a barrier before reverse-order release, parsed actual YAML,
wrote distinct synthetic artifacts in canonical collection order, and emitted one
manifest amid unterminated preceding progress and over-4000-byte following output.
Actual IsolatedCase.run! required-mode forwarding delivered the frame; real
TargetEvidence.capture and verify accepted all five artifacts after child cleanup.
The receipt is retained in aggregation-output/consumer-receipt.json.

The parent compilation reported an unused private digest function and unused case-list
attribute because the source conditionally compiles a child-only test. Those warnings
are retained as probe limitations; this was not the warnings-as-errors production gate.
No execution failure or retry occurred. The small source is a local prototype, not code
to install wholesale. It does not prove real Shaping behavior, the future driver,
all failure controls, or full publication (the latter already has accepted 06 proofs).

## Source evidence audit

All historical protocol hashes present for copied compact fixture files match the
current bytes. CORRECTION.md explicitly states that v1 reader controls remain valid
and v2 only corrects malformed scopes; earlier suspicion based solely on v1 path names
is resolved by the source hashes and that correction. No unchanged successful reader
probe was rerun. fixtures/ preserves exact sources, historical receipts and their limits.

## Consequences

Use a single isolated suite owner with five private processes and one aggregate
manifest. No new scheduler or multiple target frames. Source/root/profile/turn binding,
script routing, partial-turn capture and actual consumer controls remain explicit
Developer work, rehearsed offline before live. Autonomous probe/no-reconfirmation
requirements are now acceptance conditions, not an extra live campaign.

## Regression-target selection clarification

The Shaper asked whether unchanged live tests run when production implementation could
break them. Source inspection confirms Build selects the scenario target union; it
does not dynamically select live from code/test diffs, and check excludes live-tagged
tests. The Draft now proposes explicit impact-based Shaping target selection. It does
not claim an automatic Build-time safety net or silently add one to this scope.
