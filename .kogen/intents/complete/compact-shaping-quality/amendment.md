> Current approved revision: read [approval-transition-amendment.md](approval-transition-amendment.md) first. Earlier receipts remain historical evidence.

# Amendment after stopped Build

Status: explicitly approved in this conversation; see evidence/amendment-approval.md. Same Intent identity,
original provenance and five live cases. This file supersedes conflicting evaluation
or readiness statements from the original approval; unchanged requirements remain.

## Why the original evidence is insufficient

Build gEELGTWs_StA0ZFraHgiWw64 exhausted its two outer resumptions. Its middle live suite
passed, but Review rejected complete-case-restraint. Inspection found a missing fixture
choice and incorrect chronology in part of that judgment. The final live suite failed
with a missing-marker cleanup assertion and another failure not identifiable from the
retained tail. These are preserved observations, not acceptance or permission to retry.
The recorded verdict is not rewritten; this amendment corrects future evaluation.

## Corrected input delivery

Before launching each public Shape session, materialize an ordinary fixture-owned
current user request in README.md, linked case facts and authority, target slug, and
the request to save a reviewable Draft without approval. For continuation, the current
request names the outstanding work without supplying either withheld answer. Freeze
these exact bytes and record their availability before dispatch. The controller reads
ordinary files through the unchanged public role flow; do not append a stronger policy
or hidden rubric to the role prompt. A later chat message cannot be the first delivery
of a constraint against which earlier behavior is graded.

Remove the driver's current sequence that waits for a complete shaping turn and then
resumes merely to deliver the intended initial task/slug. The first substantive turn
must consume the prepared case request. Later replies contain only prescribed partial
or final choices, remain in that case's native session and retain actual delivery/turn
binding. Do not double-deliver the initial request as a rename rescue. Keep five roots
and one fresh seed continuation, not an extra startup shaping campaign.

Retain all visible earlier events. Bind a judgment to the supplied request/facts and
relevant read/turn events available then. A statement before a later instruction cannot
be classified as defying that instruction. An instruction supplied and ignored remains
a real failure; missing delivery is a fixture failure. Never discard a problematic
initial turn simply to make a final contract pass.

## Complete fixtures and fair questions

fixtures/facts.json now proposes explicit test-author output_contract entries: normalized
CSV uses LF with a final LF and preserves validated field text; availability prints a
JSON array of id/time pairs, with [] for empty success, followed by LF. Executable
packaging, internal architecture and insignificant JSON whitespace remain ordinary
engineering discretion. These facts belong in complete fixture input before launch.
The flawed/continuation cases keep only their specified format/recovery/setup choices
withheld. Their output representation is supplied too; it is not a new hidden question.

Do not generalize these test choices into production prompt answers. Remove the specific
JSON-versus-tab-separated/LF guidance introduced by the failed repair. General guidance
must distinguish supplied choices, material unresolved behavior and implementation
freedom. For a material missing public choice, expose the question or a proposed
alternative for the human; do not silently accept it. Reasonable implementation choices
within established constraints need no redundant confirmation. No compulsory
zero-question metric and no promise that any finite complete fixture covers all cases.

If an unexpected consequential omission remains in a complete case, preserve it as a
fixture/oracle issue requiring assessment rather than treating a necessary question as
model failure or answering it by guessing. A redundant confirmation of a fact already
supplied is still a model failure; scripts must not rescue it with a yes. An approval
request is judged against the actual request and its delivery, never treated as approval.

## Source ownership and grading controls

The first failed CSV case also changed fixture README.md. Define the evaluated writable
surface consistently: Draft/evidence under its assigned Intent and explicitly owned
probe output only. README/request/fixtures and application source remain read-only.
Acceptance requires source hashes unchanged. Do not weaken the source mutation control
or auto-revert the mutation to make the case pass. These are existing Shaping boundaries.

Offline rehearsal must exercise the changed actual driver/public dispatch/capture path,
not just the new prototype: initial request before evaluated turn; missing/late delivery
negative; a previously supplied fact redundantly reconfirmed; a genuinely missing
consequential choice correctly kept open; intermediate partial choice unchanged until
its explicit answer; source mutation rejection. Keep the distinction between executable
ordering/integrity controls and independent semantic assessment. Production prompt
regressions must reject embedding these specific fixture answers as general policy.

## Bounded cleanup repair

The evidence-local delayed-start experiment reproduces the failing assumption: a
collection timeout can fire before its expected marker exists. With equal 3.5-second
startup delay and two-second collection timeout, existing readiness handling lets the
marker exist before timed collection and still performs supervisor cleanup. This proves
a mechanism, not the exact cause or timing of the historical run.

Add only test/kogen/isolation_cleanup_test.exs and test/support/isolation_probe.exs to
editable scope. Make that test signal readiness after its PID/root prerequisites are
written, use existing bounded startup/readiness support, then exercise collection
timeout and assert actual descendant termination/root cleanup. Include deterministic
slow-start positive and never-ready negative controls. Do not merely lengthen a sleep,
skip missing-marker assertions, weaken cleanup, or modify the supervisor/Build engine.
If the minimal test-only correction is insufficient, stop and expose the extra scope.
This introduces no new live case; the existing cold suite exercises it.

## Verification, preserved evidence and recovery

Require check and live and ordinary fresh independent Review. Retain the same two outer
resumptions per new Build; do not resume/reset or rewrite the exhausted Build. The latest
five-case captures have no accepting Review and cannot substitute for the corrected run.
Preserve the second final-attempt failure as unknown unless recoverable evidence names
it. Do not skip the full live suite because one failure remains unidentified.

The failed Candidate and original Approved bytes are frozen outside controller records;
exact tracked patch and untracked archive are also supplied under evidence/recovery-inputs.
They are unaccepted source inputs, not evidence of correctness. A clean worktree at
56af370a is prepared on repair/compact-shaping-quality. On amended approval, place this
package in that worktree's ignored Approved directory. The new Developer first applies
the preserved tracked patch/untracked sources there, verifies the inventory, and repairs
them within the amended guarded paths. It must not change these frozen input artifacts
or the original failed checkout. Do not commit an unreviewed Candidate as a bootstrap.

The tracked patch dry-run passes in the clean worktree. Build's clean-tree check stays
unchanged, and no source code is changed while a Build is running. The original dirty
checkout remains preserved. Reusable repaired scripts belong in the maintained test
source, not in this amendment's evidence directory. The new Build owns production
rehearsal/gates and acceptance; current bounded probes establish only feasibility.
