# Explicit fixture choices proposed after failed Build

facts.json adds test-author output_contract entries. These close omissions in the
supposedly complete fixtures; they are not new global Shaping policy. The original
facts remain in the frozen approved-before-amendment snapshot and recovery inputs.
Historical protocol.json hashes describe that original version and cannot certify
this changed facts.json. Reader and adapter sources remain unchanged with their
previous limited receipts. Freeze a new complete fixture inventory before execution.

The general Shaping prompt must distinguish consequential choices, supplied decisions
and ordinary engineering discretion without naming the disputed JSON/TSV/LF answers.
This amendment is proposed for explicit package approval, not silently accepted policy.
