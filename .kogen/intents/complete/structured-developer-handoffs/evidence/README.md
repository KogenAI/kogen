# Shaping investigation

## Observations and limits

Inspected HEAD: `2890d86f16c8819cfad12cbd98dbbdc8ee068b1c`. Local CLI: `codex-cli 0.154.0`. README's existing verification target remains 0.153.4; this investigation does not claim to have executed that version or authorize a compatibility restriction. No application source, maintained tests, configuration or hooks were edited. All owned probe inputs/results are inside this Draft.

- [Original input](followup-input.md) is a preserved copy of the Shaper-selected follow-up.
- [Source hashes](source-hashes.json) bind the inspected producer/consumer code and config. Harness launch/resume currently call run_turn without a schema; launch_reviewer owns temporary schema/output files. Build.begin_attempt creates tokens; launch_attempt invokes both Developer routes; settle records the message and validates Check before Contract.handoff; invalid handoff reworks in the shared allowance.
- Contract.handoff and its collection/reference helpers were inspected completely for the changed boundary. They reject duplicate/omitted IDs, malformed entries, whitespace, unready states, incorrect risk links, unsafe paths, symlinks, non-regular files and missing evidence. Review remains the authority for substantive claims.
- Consumers inspected: harness args/verdict/role tests; scenario_response.py; fake_codex and schema-flag assumptions across fake providers, lifecycle counts, core integrity and verification ownership tests; native_helper_live_test.exs and NativeHelperFixture.prompt; live_shape_to_build_test.exs and its distinct connected and Reviewer-rework owners. Build is the production handoff caller; the native-helper fixture is a distinct low-level transport use and must keep its own confirmation output.
- A bounded read-only Luna-low scout investigated Contract, Build transitions and the approved Unify verification package. Root integrated those findings against source. No helper writes or native test claims were used. Root/helper usage is unavailable, not zero.

## Native probe

[probe.py](probe.py) rehearsed its deterministic first-block/second-pass hook before native execution, then ran fresh and exact-session resume with configured Developer `gpt-5.6-sol` at `low`. [rehearsal.json](rehearsal.json) records that limited rehearsal; it does not stand for provider enforcement. [native-summary.json](native-summary.json) records observed IDs, tokens and usage. Each `*.invocation.json` contains exact argv, cwd and input. `*.stdout.jsonl`, `*.stderr.txt`, `*.message.json` and `*.receipt.json` preserve native output and exit/file evidence. Schemas are `attempt-one.schema.json`, `attempt-two.schema.json` and `empty-attempt.schema.json`.

Fresh and resume succeeded, used the same session, and returned their respective bound token despite the prompt requesting stale-token and an extra key if allowed. The custom Stop callback recorded false then true stop_hook_active in the same native turn, followed by a separate resumed turn. [native-fixture/stops.jsonl](native-fixture/stops.jsonl) and [stop.py](native-fixture/stop.py) retain exact callback inputs and the deterministic block rule. This proves the bounded schema/Stop/resume transport combination, not Kogen's actual check/Review/publication route.

The original schema permitted empty implementation arrays; both native handoffs used them. This is a real limitation, not consumer success. [consumer-results.json](consumer-results.json) shows rejection by the current source-loaded Contract. [empty-probe.py](empty-probe.py) is a separate tightened fresh variant, retaining nonempty implementation/evidence arrays and zero risks/findings; its native completion succeeded with those shapes. It retains its own empty-fixture callback evidence. Do not merge these runs into a claim that a semantically valid full Kogen Build was exercised here.

[invalid-schema-control.json](invalid-schema-control.json) records a native local schema-load failure: nonzero exit with the seeded [stale.message.json](stale.message.json) unchanged. That is a valid stale-file hazard control. It is not a simulated provider interruption nor evidence of recovery. Fresh owned paths plus settlement checks are required. Missing/truncated outputs are tested directly at the current semantic consumer, while the future Harness failure matrix must also exercise transport classification.

Native stderr contains an unrelated Cloudflare MCP authentication error; no MCP tools were used, and the Codex invocations themselves returned success with turn.completed and output files. This proves access to the configured Codex model for these runs only, not general external credentials. Provider-owned session persistence remains outside the package under normal Codex storage. Necessary callback/output evidence is already retained here, so readers need not recover that mutable session store. Original absolute probe paths are historical observed argv, not current links after any package move.

## Source-bound consumer controls

[consumer-probe.exs](consumer-probe.exs) loads the actual current `lib/kogen/build/contract.ex` into memory using installed dependency code paths. [consumer-invocation.json](consumer-invocation.json) retains command, exit, stdout and warnings. It performs no compilation write, Make target or gate. Ten controls passed: native empty-reference rejection, corrected valid control, stale token, duplicate scenario, missing file, incomplete scenario, blocked finding, reversed risk links, missing message and truncated message. The valid control deliberately supplies the existing fixture source.txt reference and is labelled synthetic; it does not repair or relabel the original native evidence.

## Reproduction and cleanup

Inputs: this evidence source, Python 3, Git, installed project Elixir dependencies, Codex 0.154.0 and authenticated access to configured Developer model. Starting location: repository root. Tools: Python subprocess argv lists, Codex CLI, Elixir current-source consumer.

For a new authorized investigation, copy probe sources into a fresh owned evidence directory; never overwrite these retained receipts. Run `python3 <new-dir>/probe.py --rehearse`, inspect its hook block/pass receipt, then `python3 <new-dir>/probe.py fresh`, then `python3 <new-dir>/probe.py resume`. Each provider invocation has a 150-second subprocess bound and refuses existing final-output collisions. Run the separate empty-probe variant only when testing its additional tightened/zero-collection constraints. Preserve failed runs and stderr; inspect failures before any new attempt. Use the consumer-invocation argv pattern with current dependency paths for the source consumer controls; Python YAML was unavailable, so package validation uses installed YamlElixir, without dependency installation.

Outputs/checks: retained argv/input/schema/stdout/stderr/final bytes/exit, exact session equality, current token, block/pass same-turn observations, consumer rejection and paired valid control. A custom probe success never completes the later Build scenarios. Completion of this investigation means feasibility and limits are recorded, not implementation readiness evidence from future `check`/`live` runs.

Nested `.git` directories used only to establish fixture cwd have been removed after native settlement and receipt retention; probe scripts recreate disposable Git metadata when run in a new directory. Source.txt, hooks, outputs and callback evidence remain. Production implementation must promote any reusable verification logic into maintained test/support paths, not call this historical shaping evidence as its test suite.
