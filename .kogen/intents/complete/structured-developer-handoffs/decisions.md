# Choices and provenance

- Supplied by the Shaper: follow-up 13-structured-developer-handoffs.md, selected in this conversation, requires schema-constrained fresh/resumed Developer handoffs, preserved semantic authority, bounded native probes and separate failure classes. It explicitly leaves correction policy unsettled and excludes unified verification/budget redesign.
- Identity/baseline/profile/start timestamp come from this conversation's fresh startup and are preserved verbatim in intent.yaml. Inspected HEAD matches the supplied baseline; tracked working tree was clean before investigation.
- Source-derived: Build already binds current token, rejects unready/incomplete collections and unsafe references, snapshots exact citation bytes, and applies shared outer rework after settled handoff rejection. Reviewer schema use proves a local design precedent, not Developer-route feasibility.
- Probe-derived: CLI 0.154.0 with configured Developer gpt-5.6-sol/low accepted strict objects, enums, lengths and cardinalities on fresh and exact-session resume. A custom Stop block continued the same turn. A tightened fresh case accepted empty optional collections and nonempty reference arrays. Native stdout, schemas, outputs and callback inputs are retained.
- Negative observations remain evidence: original probe schemas permitted empty implementation arrays and native output used them; the actual consumer rejected them. Invalid schema loading exited nonzero and left the pre-existing output untouched. Neither observation is described as end-to-end success.
- Engineering decisions: retain the existing JSON wire shape and runtime validator; provide controller-owned per-attempt schema and fresh owned output files; retain evidence before cleanup; identify fake roles independently of --output-schema; preserve the distinct non-Build native-helper transport consumer. No new audience or installation limitation is introduced.
- Q1 accepted in this conversation: the Shaper replied “Keep the existing budget (recommended)”. Preserve shared outer-resumption accounting; no new correction allowance. This policy answer and the earlier Unify verification approval are not approval of this Intent. Historical Draft note: at the time Q1 was resolved, no Intent approval had been given. Current approval is maintained in intent.yaml.
