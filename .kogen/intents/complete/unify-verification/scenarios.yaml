[
  {
    "id": "hook-ownership",
    "given": "a new-controller Build declares check and ordered non-check targets with duplicates",
    "when": "the root Developer attempts to stop",
    "then": "Stop alone runs check then distinct declared targets in first occurrence order; Build imports receipts and does not rerun them; check-only Intents never run undeclared live",
    "wrong_result": "the prohibited-target list is executed or Build duplicates live",
    "verified_by": [
      "check",
      "live"
    ],
    "evidence": "Production-hook public fixture asserts executable owner and exact sequence; native public Build driver retains calls."
  },
  {
    "id": "shared-failures",
    "given": "verification_retries is 2 and check fails once followed by live failing after check passes",
    "when": "the next complete cycle also fails any required target",
    "then": "V progresses 1,2,3 without an intermediate reset and Build terminates with no fourth callback/resume/Review",
    "wrong_result": "each target has its own allowance or passing check resets live failures",
    "verified_by": [
      "check"
    ],
    "evidence": "Mixed-target deterministic hook/Build history and negative fourth-call marker. Generate prior failed cycles through the production hook; corrupt/delete state and assert zero subsequent dispatch rather than reset-to-one."
  },
  {
    "id": "third-pass",
    "given": "two cycles have failed before the first Review",
    "when": "the third cycle passes every gate and Review requests repair",
    "then": "V resets to zero, outer repair 1 resumes the same Developer, and another two verification failures followed by complete pass are allowed",
    "wrong_result": "the old total counter rejects the first Review repair",
    "verified_by": [
      "check",
      "live"
    ],
    "evidence": "Combined public Build fixture and native live route retain per-cycle receipts, same session and fresh Review."
  },
  {
    "id": "outer-limit",
    "given": "all handoffs are valid and full verification passes between Review verdicts",
    "when": "Review returns rework three times",
    "then": "the first two verdicts resume the Developer and the third ends Build, preserving findings; V resets never reset the outer counter",
    "wrong_result": "Review retries are renewed by each passing verification",
    "verified_by": [
      "check"
    ],
    "evidence": "Deterministic three-Review trace, no fourth Reviewer/Developer resume and no publication; accepting-third control."
  },
  {
    "id": "handoff-preservation",
    "given": "the installed controller-owned handoff schema is active and complete verification passes, but final output is structurally missing/invalid or semantically unready",
    "when": "Build validates the handoff",
    "then": "existing schema constraints and semantic authority remain; settled invalid handoffs retain the accepted shared outer correction policy, while each resume has the same Developer and fresh schema/token/output path; no new pool or unconstrained fallback exists",
    "wrong_result": "successful tests are treated as proof the handoff must be valid or malformed messages retry forever",
    "verified_by": [
      "check",
      "live"
    ],
    "evidence": "Current structured-output failure matrix and semantic controls through public Build; retained native fresh/resume invocation audits survive gate relocation. Correct filename plus locator is paired with a nonexistent filename containing :line; retain the installed reference contract."
  },
  {
    "id": "regression-bound",
    "given": "Review-directed changes regress check or a later target",
    "when": "repeated repairs alternate targets and Review",
    "then": "every Stop starts all gates afresh, partial target success never resets V, and both allowances enforce the finite bound even with unchanged Candidate",
    "wrong_result": "the loop skips previously passing targets or refreshes outer budget",
    "verified_by": [
      "check"
    ],
    "evidence": "Table-driven actual-controller sequences include nine-cycle upper-bound case and unchanged-Candidate failures. Roll back latest state with later receipt history retained and prove no extra target dispatch or allowance."
  },
  {
    "id": "native-exhaustion",
    "given": "a schema-constrained native Build Developer receives two Stop blocks then a third verification failure",
    "when": "the hook writes terminal exhaustion then returns continue:false",
    "then": "the invocation ends and fresh bound terminal exhaustion stops Build before successful handoff processing or missing/empty/malformed/truncated-output rework; Codex exit 0 and valid final JSON grant no further attempt or Review; replaying even an intact exhausted callback dispatches zero targets, as do deleted/corrupt exhausted-state controls",
    "wrong_result": "current structured_output error branches resume the exhausted Developer, or a semantically valid handoff overrides failed gates",
    "verified_by": [
      "check",
      "live"
    ],
    "evidence": "Actual production-hook public Build exhaustion and passing controls; source-bound shaping Harness probe demonstrates valid JSON after continue:false but is not full-route acceptance. Offline production-hook replay tests must assert no fourth dispatch, not merely continue:false in the response."
  },
  {
    "id": "evidence-forwarding",
    "given": "a hook-owned target emits a valid required manifest and artifact",
    "when": "it completes and a later target fails before a subsequent repair cycle",
    "then": "manifest and required bytes were captured before continuation, retained across failures and forwarded through Build/Review/Complete; missing/changed/invalid sources fail existing evidence validation",
    "wrong_result": "artifact collection waits until after Developer continuation or loses uncited bytes",
    "verified_by": [
      "check",
      "live"
    ],
    "evidence": "Actual TargetEvidence controls and real required-evidence fixture through the new consumer chain."
  },
  {
    "id": "binding-integrity",
    "given": "the production unified initializer has created state and real hook callbacks have produced failed cycles, exhaustion, or later history",
    "when": "state is deleted, unreadable, malformed, wrong-bound, internally inconsistent, or rolled back while later history remains, and the registered Stop hook is invoked",
    "then": "validation rejects the callback before any Make target runs; dispatch markers stay unchanged, no empty history is substituted, evidence remains inspectable, and public Build terminates without further repair, Review or publication",
    "wrong_result": "catch-all state loading resets history, or rejection occurs only after a target has already dispatched",
    "verified_by": [
      "check"
    ],
    "evidence": "Production initializer plus registered-hook mutation matrix, generated real prior cycles, before/after dispatch markers, and actual Build no-resume/Review/commit assertions; stopped Candidate probe provides a failing reference."
  },
  {
    "id": "preserved-authority",
    "given": "Build reworks and later accepts a verified Candidate",
    "when": "history and Complete evidence are produced",
    "then": "same Developer, fresh independent Review, finding origins/closures, role-specific snapshots and all failed-cycle history remain inspectable even without raw logs; exact structured schema/message bytes and their hashes survive cleanup and remain bound to each outer attempt",
    "wrong_result": "new counters erase failed attempts or grant the Developer control of authoritative records",
    "verified_by": [
      "check",
      "live"
    ],
    "evidence": "Tracking and publication fixtures plus native lifecycle audit of exact sessions."
  },
  {
    "id": "config-contract",
    "given": "explicit verification_retries and outer_resumptions are supplied or invalid/missing",
    "when": "Build preflights configuration",
    "then": "both accept nonnegative integers, invalid config fails before provider dispatch, 0 stops first failure and 2 permits two repairs; unrelated profile settings remain unchanged",
    "wrong_result": "a missing allowance is silently invented or interpreted as total attempts",
    "verified_by": [
      "check"
    ],
    "evidence": "Config/parser and public precondition tests, config producer audit, zero/two allowance controls."
  },
  {
    "id": "self-build-transition",
    "given": "the accepted c1c9d137 controller remains loaded while the candidate hook is installed, and separately a freshly loaded candidate controller runs its hook",
    "when": "the initial attempt passes and Review rework resumes the same Developer with fresh attempt bindings",
    "then": "both supported initial/resumed routes settle through their actual consumers without duplicate targets; the accepted controller retains legacy ownership, the candidate controller uses initialized unified state/history, and incompatible intermediate unified context fails before dispatch without downgrade or reset",
    "wrong_result": "fresh/new component tests pass while a retained controller emits context incompatible with the edited hook; missing state is fabricated to conceal the mismatch",
    "verified_by": [
      "check"
    ],
    "evidence": "Existing deterministic self-build fixture retains its launching controller across hook replacement and Review rework; separate fresh candidate initial/resume control; observed missing-history context negative control with zero-dispatch marker. Retain protocol/source provenance and actual consumer settlement or admission diagnostics. See INTENT.md controller-lifetime subsection."
  },
  {
    "id": "lifetime-isolation",
    "given": "live runs inside Stop and starts nested private fixtures under inherited environment",
    "when": "fixtures execute, or a scaled timeout interrupts their owned processes",
    "then": "live keeps its existing time envelopes, nested counters/roots stay private, descendants settle, incomplete hooks cannot pass, and no full live recursion occurs",
    "wrong_result": "600-second cutoff truncates supported live work or nested Build rewrites parent state",
    "verified_by": [
      "check",
      "live"
    ],
    "evidence": "Production timeout/cleanup controls with short deadlines and actual native/cold-offline/live workflow evidence."
  },
  {
    "id": "gate-guard",
    "given": "Developer or delegated helper attempts a protected explicit Make/Stop invocation",
    "when": "PreToolUse dispatches the request",
    "then": "existing covered forms remain denied before execution while focused non-gate work and genuine root Stop callbacks work",
    "wrong_result": "moving ownership grants manual gate permission or helper Stops run parent gates",
    "verified_by": [
      "check"
    ],
    "evidence": "Production guard regression plus public root/helper-context fixture with forbidden execution markers."
  },
  {
    "id": "receipt-consumers",
    "given": "a current unified public Build has completed a real passing Stop cycle",
    "when": "the existing Shape-to-Build audit and other receipt consumers inspect Check and target evidence",
    "then": "required receipt fields including Check finished_at retain valid values and meaning bound to the actual cycle; the unchanged timestamp consumer parses successfully and Review/Complete retain the same evidence",
    "wrong_result": "a new receipt drops finished_at, substitutes a dummy value, or weakens the consumer assertion to make live pass",
    "verified_by": [
      "check",
      "live"
    ],
    "evidence": "Focused production receipt-to-existing-consumer test before paid runs, plus real Shape-to-Build and retained timestamps in its public record."
  },
  {
    "id": "shaping-state-guardrails",
    "given": "fresh or continued public Shape handles a source-backed feature whose state gates a side effect",
    "when": "the maintained shaping instructions and flawed/complete guardrail pair are exercised",
    "then": "instructions reach both routes, the flawed native Draft captures initialization and pre-action validation plus concrete zero-dispatch and valid-repair controls, and the complete Draft preserves supplied behavior without unnecessary scope questions; both remain unapproved",
    "wrong_result": "only prompt keywords change, an error-after-dispatch is accepted, or the process demands unrelated checks for every feature",
    "verified_by": [
      "check",
      "live"
    ],
    "evidence": "Fresh/continued prompt delivery, real fixture mutation/action controls, actual paired native Drafts and independent semantic assessment against retained bad-contract and valid controls."
  },
  {
    "id": "shaping-consumer-evidence",
    "given": "a fixture producer changes its receipt and the existing source consumer requires finished_at, while a callback-only success is supplied",
    "when": "public Shape prepares the fixture Intent and evaluation evidence is delivered",
    "then": "the Draft preserves the source consumer contract with focused observable checks and distinguishes feasibility from full-route evidence; actual paired captures and counterexamples reach Review through one aggregate manifest without dropping existing cases",
    "wrong_result": "a dropped consumer field is ignored, its assertion is weakened, or a passing transport probe is claimed as full verification",
    "verified_by": [
      "check",
      "live"
    ],
    "evidence": "Producer-to-real-consumer positive/negative controls, deterministic capture/manifest corruption controls, retained native brief pair and semantic Review; preserve the five existing evaluation cases."
  }
]
