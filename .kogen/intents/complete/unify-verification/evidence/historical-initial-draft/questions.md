# Unresolved choices

This package is an incomplete, unapproved Draft. No policy has been selected yet.

1. Repair policy: recommended one-time fresh allowance at first Review eligibility (matching Check, valid handoff, and all required targets passed). Two resumptions before, two after; later regressions spend the post-milestone remainder. Alternatives: separate failure-category pools, or once-per-new-stage allowances. This determines whether regression repair remains possible after exhausting initial verification repair and the maximum total work. Asked in this shaping conversation; awaiting the Shaper.
2. Configuration compatibility: current `outer_resumptions` means a global maximum. The selected policy needs an explicit configuration contract; do not silently reinterpret existing configurations. Resolve after the policy choice, including concrete allowances and legacy behavior.
